This directory contains archived SQL files moved from `../`.

Files moved here are to be retained for reference but are not used by the current Firebase-based setup.

If you need to restore any file, move it back to `scripts/`.
